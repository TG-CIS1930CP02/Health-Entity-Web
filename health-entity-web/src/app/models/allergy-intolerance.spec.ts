import { AllergyIntolerance } from './allergy-intolerance';

describe('AllergyIntolerance', () => {
  it('should create an instance', () => {
    expect(new AllergyIntolerance()).toBeTruthy();
  });
});
